import { Spinner } from '../spinner/Spinner';
import './auth.css';

const AuthComponent = () => {
  return (
    <div className="auth-container">
      <Spinner />
    </div>
  );
};

export default AuthComponent;
